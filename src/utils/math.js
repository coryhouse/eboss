// Pure Functions
// Relies only on the arguments passed in.
// Doesn't mutate its arguments
// Returns a new value
// Will always return the same result for a given input
export function add(x, y) {
  return x + y;
}

export function subtract(x, y) {
  return x - y;
}
